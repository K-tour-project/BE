# 01. 데이터 정의서

> 원칙: **"무엇을 우리가 큐레이션하고, 무엇을 TourAPI가 채우는가"**를 필드 단위로 구분한다.
> 🟦 = 우리가 직접 구축/큐레이션(핵심 자산) · 🟩 = TourAPI에서 동기화 · ⬜ = 시스템 생성

---

## A. 엔티티 개요
| 엔티티 | 역할 | 출처 |
|---|---|---|
| `title` (작품) | 영화/드라마/예능 메타 | 🟦 + KMDb/TMDB |
| `place` (장소) | 촬영지·연관지·관광지 | 🟦(존재) + 🟩(여행정보) |
| `title_place` (작품↔장소) | **연결 레이어 = 프로젝트 핵심** | 🟦 |
| `course` / `course_item` (코스) | 사용자가 만든 동선 | ⬜ |
| `region` (지역코드) | TourAPI areaCode/sigunguCode 매핑 | 🟩 |
| `app_user` (사용자) | 저장 기능용(선택) | ⬜ |

---

## B. 필드 상세

### 1) `title` — 작품
| 필드 | 타입 | 출처 | 설명 |
|---|---|---|---|
| id | PK | ⬜ | |
| type | enum(movie/drama/show) | 🟦 | 유형 |
| title_ko | text | 🟦 | 작품명(국문) |
| title_en | text? | 🟦 | 다국어 확장 대비 |
| release_year | int | 🟦/KMDb | 공개연도 |
| poster_url | text | KMDb/TMDB | 포스터 이미지 |
| synopsis | text | 🟦/KMDb | 간단 정보 |
| genres | text[] | KMDb/TMDB | 장르 |
| kmdb_id / tmdb_id | text? | 외부 | 외부 식별자(중복방지·메타보강) |
| era | text? | 🟦 | 사극 시대(예: 조선) — **폴백 추천용** |
| keywords | text[] | 🟦 | 인물·사건·배경 키워드 — **폴백 추천용** |
| popularity | int | ⬜ | 인기 정렬용 |

### 2) `place` — 장소
| 필드 | 타입 | 출처 | 설명 |
|---|---|---|---|
| id | PK | ⬜ | |
| name | text | 🟦/🟩 | 장소명 |
| geom | geography(Point,4326) | 🟩 mapx/mapy | 좌표(경도/위도) |
| address | text | 🟩 addr1 | 주소 |
| area_code / sigungu_code | text | 🟩 | 지역코드 |
| tour_content_id | text? | 🟩 contentid | **TourAPI 조인키** |
| tour_content_type_id | int? | 🟩 contenttypeid | 12관광지/14문화시설/39음식 등 |
| category | text | 🟩 cat1~3 | 분류 |
| thumbnail_url | text | 🟩 firstimage | 대표 이미지 |
| overview | text | 🟩 detailCommon | 개요 |
| tel / homepage | text | 🟩 detailCommon | 연락처/홈페이지 |
| use_time | text | 🟩 detailIntro | 운영시간 |
| rest_date | text | 🟩 detailIntro | 휴무일 |
| source | text | 🟦 | 데이터 출처 표기 |
| tour_synced_at | timestamp | ⬜ | TourAPI 동기화 시각 |

### 3) `title_place` — 작품↔장소 (M:N, **핵심 자산**)
| 필드 | 타입 | 출처 | 설명 |
|---|---|---|---|
| id | PK | ⬜ | |
| title_id / place_id | FK | 🟦 | |
| relation_type | enum | 🟦 | 아래 6종 |
| scene_description | text | 🟦 | 촬영 장면/관련 설명 |
| relevance_reason | text | 🟦 | 추천 이유(상세화면 노출) |
| confidence | enum(verified/likely/inferred) | 🟦 | 검증 수준 |
| source_url | text | 🟦 | 출처 링크 |
| — | UNIQUE(title_id, place_id) | | 중복 방지 |

**relation_type 6종(유저스토리 4 기반):** `filming_site`(실제 촬영지) · `filming_candidate`(촬영지 후보) · `background`(작품 배경지) · `historical`(역사 연관지) · `fan_spot`(팬 방문지) · `nearby_recommend`(주변 추천지)

### 4) `course` / `course_item` — 코스
- **course**: id, user_id?(익명 허용), name, transport_mode(car/transit/walk), start_geom?, total_distance_m, total_duration_min, created_at
- **course_item**: id, course_id(FK), place_id(FK), visit_order(int), stay_minutes, travel_to_next_min

### 5) `region` — 지역코드 매핑
- area_code, sigungu_code, name, parent_name(시도), centroid_geom?

### 6) `app_user` — 사용자(선택, MVP는 익명 가능)
- id, email, nickname, created_at  *(Supabase Auth 쓰면 auth.users로 대체)*

---

## C. TourAPI 필드 매핑 (place를 무엇으로 채우나)
> TourAPI 4.0(국문 관광정보 서비스_GW). 오퍼레이션 접미사(`2`)는 구독 키 버전 확인 필요.

| 우리 필드 | TourAPI 오퍼레이션 | 응답 필드 |
|---|---|---|
| tour_content_id, name, geom, thumbnail | `areaBasedList2` / `locationBasedList2` / `searchKeyword2` | contentid, title, mapx, mapy, firstimage |
| area_code, sigungu_code, content_type_id, category | (위와 동일) | areacode, sigungucode, contenttypeid, cat1~3 |
| overview, tel, homepage | `detailCommon2` | overview, tel, homepage |
| use_time, rest_date | `detailIntro2` | usetime, restdate (타입별 필드 상이) |
| 이미지 갤러리 | `detailImage2` | originimgurl |
| 주변/연관 장소 | `locationBasedList2`(반경) / 관광지별 연관관광지 API | contentid 목록 |
| 코드 기준정보 | `areaCode2`, `categoryCode2` | 지역/분류 코드 |

---

## D. 연결 데이터(title_place) 수집 파이프라인 — 가장 중요
TourAPI에 없으므로 **우리가 만든다.** 추천 단계:
1. **시드 큐레이션**: 타깃 지역/인기작부터 수작업으로 작품↔장소 시드 셋 작성(스프레드시트).
2. **메타 보강**: KMDb/TMDB로 포스터·연도·장르 자동 채움.
3. **장소 매칭**: 장소명/주소 → 지오코딩 → TourAPI `searchKeyword2`로 contentid 매칭(없으면 좌표만 보유).
4. **검수**: confidence 등급 부여(verified/likely/inferred), 출처 기록.
5. **동기화 배치**: 매칭된 contentid로 detailCommon/Intro/Image 주기적 캐시.

> ⚠️ TourAPI를 매 요청마다 호출하지 말 것 — 레이트리밋·지연. 우리 DB에 캐시 후 배치 동기화.
