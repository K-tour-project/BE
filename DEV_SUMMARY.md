# K-tour BE — 개발 요약 (처음부터 지금까지)

> 영화·드라마 촬영지 기반 관광 코스 앱 **Every Trip**의 백엔드.
> 이 문서는 **"원래 뭘 하려 했고 → 무슨 기술을 왜 골랐고 → 도중에 뭐가 바뀌었고 → 지금 어디까지 왔는가"**를
> 한 번에 읽는 용도다. 작업 방식(워크플로우)과 구조(설계도)는 §5·§6에 있다.
>
> 작성 2026-08-22 · 마감까지 **30일**(1차 서류 2026-09-21 16:00)
>
> 다른 문서와의 관계 — 단계별 현황 스냅샷은 [`PROGRESS.md`](./PROGRESS.md), 판단 근거 상세는
> [`DEVELOPMENT_LOG.md`](./DEVELOPMENT_LOG.md), 팀과의 API 약속은 [`API_CONTRACT.md`](./API_CONTRACT.md).

---

## 0. 지금 위치 (30초 요약)

| | |
|---|---|
| **완료** | 0 · 1 · 2 · 2.5 · **3** · 4 · 5 단계 |
| **남음** | 6(코스 추천) · 8(테스트·하드닝) · 9(배포·제출) |
| **동작 중인 엔드포인트** | **19종** (검색·지도 9 + 인증 10) + 헬스체크 |
| **DB** | PostgreSQL 16 + PostGIS, **10테이블**, 리비전 `9a1c7d2e5b40` |
| **실데이터** | 작품 1,694 · 장소 9,811 · **작품↔장소 매핑 13,755** |
| **합격 핵심 요건** | ✅ 충족 — TourAPI 실시간 호출 + `api_call_logs`로 입증 |
| **브랜치** | 백엔드 2인 분업 — `eunseo`(인증) / `yoon`(지도·지역) |

---

## 1. 원래 계획

### 만들려던 것
사용자가 **작품 제목** 또는 **지역**으로 검색하면 → 지도에 촬영지와 포스터 마커가 뜨고 →
가고 싶은 곳을 여러 개 고르면 → **최적 동선으로 관광 코스를 짜주는** 앱.
자연어 챗봇 입구는 팀원이 별도 서비스로 담당한다.

### 이 앱의 전제 (프로젝트 전체를 지배하는 사실 하나)

> **한국관광공사 TourAPI에는 "영화 ↔ 촬영지" 매핑이 없다.**

직접 조사해서 확인한 사실이고, 여기서 두 가지가 따라 나온다.

1. **작품↔장소 연결(`content_place_mappings`)은 우리가 직접 큐레이션해야 한다** → 이게 앱의 차별점이자 핵심 자산.
2. **TourAPI는 "장소의 여행정보"(주소·운영시간·이미지) 백본으로만 쓴다.**

이 전제가 데이터 모델, API 설계, 심지어 공모전 컴플라이언스 대응까지 전부 결정했다.

### 9단계 로드맵 (초기 수립)

```
0  설계          1  서버 뼈대       2  DB + 모델
3  회원가입/로그인 4  place-detail ⚠️  5  검색·지도 엔드포인트
6  코스 추천      7  AI 연동 계약     8  테스트·하드닝      9  배포·제출
```

⚠️ 4단계에 별표가 붙은 이유는 §2의 컴플라이언스 때문이다. **이게 안 되면 심사에서 제외된다.**

### 설계 v1 → v2 → v3

| 버전 | 내용 |
|---|---|
| v1 | **Supabase** 기반 (관리형 PostgreSQL + Auth + Edge Functions), 6테이블 |
| v2 | **적대적 검증** — 공모전 규정을 다시 읽고 실격 리스크를 잡아냄 |
| v3 | **FastAPI + 자체 호스팅 PostgreSQL + 자체 인증**으로 전환 ← 현행 |

문서에 "Supabase를 왜 버렸는가"가 한 문장으로 명시돼 있지는 않다. 다만 v2가 *"적대적 검증·실격리스크 수정"*
단계였고, 그 결과로 굳어진 제약 3가지 — **실시간 호출 입증 · 응답 무캐싱 · 인증키 서버 전용** — 는
전부 **"서버를 우리가 직접 소유해야 통제 가능한 것"**이다. 전환의 실질적 이유가 여기 있다고 보는 게 자연스럽다.

---

## 2. 공모전 제약 — 설계를 강제한 7가지

기술 선택의 절반은 이 제약에서 나왔다. 특히 1·2번은 **실격 직결**이다.

| # | 제약 | 설계에 미친 영향 |
|---|---|---|
| 1 | **TourAPI를 실시간 호출**하고 입증해야 한다. 파일 데이터만 쓰면 **심사 제외** | `api_call_logs` 테이블 + 모든 호출을 클라이언트 내부에서 자동 기록 |
| 2 | **API 응답 무캐싱** | `places`에서 개요·전화·운영시간·대표이미지 컬럼을 **아예 안 만듦** |
| 3 | `serviceKey`는 **서버 전용**, 앱에 절대 노출 금지 | `.env`에만 보관, 로그·`request_params`에서 제외 |
| 4 | 이미지는 **URL 표시만**, 다운로드 금지 | `poster_url`에 URL 문자열만 저장 |
| 5 | **GPS 좌표를 서버로 보내면** 위치기반서비스 사업자 등록 필요 | 입력은 `place_id`/`region_id` 정수만. 반경 조회는 **≤20km** |
| 6 | 금지 API — 산악관광정보, `areaCode2`, `categoryCode2` | `regions`를 **시드 테이블**로 만들어 코드를 미리 채움 |
| 7 | 원천 데이터 무수정 | 우리 큐레이션과 공사 데이터를 섞되 공사 값은 그대로 |

> 2번이 특히 중요하다. **옛 설계에는 TourAPI 응답을 저장하는 컬럼이 있었고 그게 곧 실격 사유였다.**
> v2의 적대적 검증에서 이걸 잡아내 v3에서 컬럼을 제거했다.

---

## 3. 기술 선택과 이유

| 영역 | 선택 | 왜 이걸 골랐나 |
|---|---|---|
| 언어·프레임워크 | **Python + FastAPI** | ① 외부 API를 **비동기로** 여러 개 동시에 부르는 게 이 앱의 주 업무다 ② `/docs` 자동 생성 → 팀원이 서버 코드를 안 보고도 테스트 가능 ③ Pydantic이 요청 검증을 스키마로 해결 |
| DB | **PostgreSQL + PostGIS** | 세 가지를 **한 DB에서** 동시에 해야 했다 — 작품↔장소 M:N 관계 · "반경 20km 안의 촬영지" 지오 쿼리 · 한글 부분검색. PostGIS가 아니면 지오 쿼리를 애플리케이션에서 손으로 계산해야 한다 |
| DB 구동 | **Docker Compose** (`postgis/postgis:16-3.4`) | `docker compose up -d` 한 줄로 PostGIS 내장 DB가 통째로 뜬다. PostGIS를 손으로 설치하면 인코딩·확장설치에서 막히기 쉽다 |
| ORM | **SQLAlchemy 2.0 (async)** | FastAPI가 async라 DB도 async여야 이점이 살아난다. 드라이버는 `asyncpg` |
| 마이그레이션 | **Alembic** | 스키마 변경을 코드로 남긴다. 팀원 DB와 내 DB가 같은 상태임을 보장하는 유일한 수단 |
| 지오 타입 | **GeoAlchemy2** (`geography(Point,4326)`) | 위경도를 숫자 2개가 아니라 **거리 계산이 되는 타입**으로. 4326 = GPS 표준 좌표계 |
| HTTP 클라이언트 | **httpx (async)** | TourAPI·구글·카카오 호출. `requests`는 동기라 이벤트 루프를 멈춘다 |
| 인증 | **자체 JWT** (`python-jose`) + **bcrypt** | Supabase Auth를 안 쓰기로 하면서 직접 구현. bcrypt는 §4의 변경으로 뒤늦게 추가 |
| 설정 | **pydantic-settings** | 비밀값(DB 비번·API 키·서명 키)을 코드에서 분리해 `.env` 한 곳으로 |

### 골랐지만 안 쓴 것 / 버린 것

| | 왜 안 썼나 |
|---|---|
| **Supabase** | §1 참조. 실시간 호출 입증·무캐싱·키 관리를 우리가 통제해야 했다 |
| **passlib[bcrypt]** | 파이썬 인증 예제의 표준처럼 쓰이지만 **유지보수가 사실상 멈췄고** 최신 bcrypt와 버전 경고가 난다. 우리가 쓰는 함수는 `hashpw`·`checkpw` 둘뿐이라 래퍼가 감춰줄 복잡함이 없다 → `bcrypt` 직접 사용 |
| **TourAPI 응답 캐싱** | 규정상 비권장. 하려면 운영계정 신청 시 별도 승인이 필요하다 |
| **`locationBasedList2`** (좌표 기반 조회) | §4 참조 — 관광지(`contenttypeid=12`)를 반환하지 않아 애초에 쓸 수 없었다 |

---

## 4. 진행하면서 바뀐 것 ★

**이 절이 이 문서에서 제일 중요하다.** 계획대로 간 것보다 바꾼 것이 결과를 더 크게 좌우했다.

### ① 순서 변경 — 3단계(로그인)를 건너뛰고 데이터 적재를 먼저 했다

원래 로드맵은 `2 → 3(로그인) → 4 → 5`였는데, **2.5단계(실데이터 적재)를 끼워 넣고 3단계를 뒤로 미뤘다.**

**왜** — 2단계가 끝났을 때 DB가 **빈 껍데기(7테이블 전부 0행)**였다. 이 상태로는 4·5·6단계에서 뭘 만들어도
검증이 불가능하다. 검색 API를 만들어도 결과가 항상 빈 배열이고, 지도 API를 만들어도 마커가 0개다.
게다가 **데이터 모양이 API 설계를 결정한다** — API를 먼저 만들면 나중에 데이터에 맞춰 다시 고쳐야 한다.

**결과** — CSV 13,761행을 4개 테이블로 분해 적재. 이후 4·5단계를 **진짜 데이터로 검증**하며 만들 수 있었다.
(예: `q=기생충` → 촬영지 28곳, 강릉 반경 5km → 44곳)

### ② 테이블 12개 → 활성 7개 → 현재 10개

**왜 줄였나** — "꼭 필요한 것만" 원칙. 컬럼·테이블은 나중에 마이그레이션 한 줄로 추가할 수 있지만,
안 쓰는 테이블을 유지보수하는 비용은 매 단계 발생한다. 모델 코드는 남겨두고
`app/models/__init__.py`의 import만 주석 처리하는 방식으로 **"지연"** 시켰다.

**왜 다시 늘었나** — 해당 기능을 실제로 만들 때 되살렸다.
`api_call_logs`(4단계, 호출 입증) · `refresh_tokens`·`email_verifications`(3단계, 로그아웃·이메일 인증).

### ③ 폴더 구조: 계층별 → 기능별

```
[전]  routers/  services/  schemas/       [후]  features/contents/{router,service,schema}.py
      catalog.py 하나에 444줄                    features/places/   features/regions/
      (작품·지역·장소 질의가 다 섞임)              features/auth/     features/health/
```

**왜** — `services/catalog.py`가 444줄까지 자라 있었고, 3·6·7단계가 들어오면 계속 비대해지는 구조였다.
**기능 하나 = 폴더 하나**로 바꾸니 새 기능 추가가 "폴더 하나 + `main.py`에 한 줄"이 됐다.
인증(3단계)이 이 구조의 첫 수혜자다 — 파일 5개가 전부 `features/auth/` 안에 들어갔다.

> `app/models/`만 옮기지 않았다. Alembic이 여기를 읽고, `mappings`가 `contents`·`places`를 동시에
> 참조해 **어느 기능에도 속하지 않기** 때문이다.

### ④ TourAPI 매칭 전략: 좌표 기반 → 이름 기반 + 좌표 검증

**왜 바꿨나** — 원래는 "우리 촬영지 좌표로 주변 관광지를 조회해서 매칭"할 계획이었는데,
**`locationBasedList2`가 관광지(`contenttypeid=12`)를 반환하지 않는다는 것**을 실호출로 발견했다.
계획 자체가 성립하지 않았다.

**어떻게 고쳤나** — `searchKeyword2`(이름 검색)를 주 수단으로 하고 좌표는 **검증**에만 쓴다.
그런데 이름 검색만으로는 오탐이 심했다.

```
서울역 → 게스 롯데아울렛 서울역점 (95m)   ← 아울렛 안 옷가게
서울역 → 다이소 서울역점          (95m)   ← 생활용품점
```

→ **이름 유사도 0.7 이상**을 추가 조건으로 걸어 차단(게스 0.43·다이소 0.60 탈락).
반대로 좌표 검증 500m는 **너무 좁았다** — 올림픽공원은 우리 촬영지점과 공사 대표좌표가 663m 벌어져
탈락하고 있었다. 넓은 장소(공원·휴양림·궁궐)는 원래 그렇다. **1km로 완화.**

**결과** — 매칭 적중률 47% → **69%**. 미검출 나머지는 방송사 사옥·촬영 스튜디오·세트장·병원으로
**애초에 관광지가 아니다.** 계약서의 *"`detail`은 null일 수 있다"*가 예외가 아니라 **정상 경로**임이 확인됐다.

### ⑤ 인증: 소셜 전용 → 소셜 + 일반 회원가입(이메일 인증 포함)

**왜** — 사용자 요구가 바뀌었다. 원래는 "소셜 2종만, 이메일/비번 없음"으로 확정돼 있었고
`AuthProvider`에서 `local`을 제거하고 `passlib/bcrypt`도 쓰지 않기로 했었다.

**영향** — enum에 `local`을 되살리고(PostgreSQL enum 값 추가는 까다롭다 → §6 함정 참조),
`users`에 `email`·`password_hash`·`email_verified`를 추가하고, bcrypt를 다시 들였다.
이메일 인증(6자리 코드) 테이블도 새로 만들었다.

### ⑥ 토큰: 14일짜리 JWT 1개 → access 1시간 + refresh 30일

**왜** — 로그아웃을 넣기로 한 순간 기존 구조가 성립하지 않았다.
JWT는 서버가 저장하지 않고 서명만 검증하므로 **"이 토큰 무효"라고 표시할 데가 없다.**
로그아웃 버튼을 눌러도 서버가 할 수 있는 게 없고, 유출된 토큰은 만료까지 살아 있다.
게다가 토큰이 하나면 **"짧게 하면 자꾸 로그아웃 / 길게 하면 위험"** 딜레마에 갇힌다.

| | access | refresh |
|---|---|---|
| 정체 | JWT | 난수 48바이트 |
| 수명 | 1시간 | 30일 |
| 저장 | 안 함 | `refresh_tokens`에 **SHA-256 해시로** |
| 취소 | 불가 | 가능 → **이게 로그아웃** |

파생 결정 둘 — **회전**(재발급 시 옛 refresh 즉시 폐기)과 **재사용 감지**(폐기된 refresh가 다시 오면
탈취로 보고 그 계정 전 세션 차단).

### ⑦ 팀 분담: 백엔드 1인 → 2인 브랜치 분리

**왜** — 팀원이 지도 API 기반 지역 구분을 맡기로 하면서 `main` 직접 커밋을 멈췄다.
인증은 다른 기능과 파일이 거의 안 겹쳐(`models/user.py`·`deps/`·`main.py` 셋뿐) **병렬 작업에 가장 안전한 단위**였다.

| 담당 | 범위 | 브랜치 |
|---|---|---|
| 김은서 | 인증 — 회원가입·로그인·로그아웃·소셜 | `eunseo` |
| 팀원 | 지도 API 기반 지역 구분 | `yoon` |

---

## 5. 워크플로우 ★

### 5-1. 프로젝트 전체를 관통하는 원칙

> **되돌리기 비싼 것부터 확정한다.**

비용 순서가 곧 작업 순서다.

```
비쌈 ↑   설계 결정 ─── 팀과의 API 계약 ─── DB 스키마 ─── 코드 ─── 문서   ↓ 쌈
        (바꾸면 전부   (바꾸면 프론트가   (바꾸면 팀원   (파일    (혼자
         다시 함)       깨짐)             DB까지 영향)   몇 개)   고침)
```

이 원칙에서 나온 실제 판단 세 가지:

- **갈림길은 코드 한 줄 쓰기 전에 확정한다.** 3단계에서 로그아웃 방식·소셜 연동 방식·이메일 인증 여부·
  브랜치를 먼저 정했다. 특히 소셜 연동 방식은 *"프론트가 앱이냐 웹이냐"*가 답을 정하는데,
  잘못 고르면 만든 걸 통째로 버린다.
- **계약서를 코드보다 먼저 쓴다.** 5단계는 `API_CONTRACT.md`를 확정하고 그대로 구현했다.
  덕분에 프론트·AI 담당이 **서버가 완성되기 전에** 목(mock)으로 개발을 시작할 수 있었다.
- **스키마 변경은 가장 싼 시점에 몰아서 한다.** 2단계에서 UI mockup과 모델을 대조해
  빠진 필드를 **마이그레이션 직전에** 전부 반영했다.

### 5-2. 한 단계를 진행할 때의 반복 루틴

매 단계 이 8스텝을 돈다. 3단계(인증)를 예로 든다.

| # | 스텝 | 3단계에서 실제로 한 일 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | **현황 파악** | 기존 코드·계약서·DB 상태를 먼저 읽음 | 팀과 이미 합의된 계약이 있으면 그게 출발점. 모르고 만들면 프론트 코드를 깨뜨린다 |
| 2 | **갈림길 확정** | 로그아웃 방식·소셜 방식·이메일 인증·브랜치 4개 | 나중에 바꾸면 스키마와 계약서를 같이 뜯어야 하는 것들 |
| 3 | **브랜치 분리** | `eunseo` 생성 | 커밋을 시작한 뒤 옮기는 것보다 시작 전이 훨씬 싸다 |
| 4 | **모델 → 마이그레이션** | `User` 확장 + 테이블 2개 + enum에 `local` | 스키마는 늦게 바꿀수록 비싸다(팀원 DB까지 영향) |
| 5 | **아래에서 위로 구현** | `core/security.py` → `service.py` → `router.py` | 위(라우터)부터 만들면 아래가 없어 가짜 값으로 때워야 한다 |
| 6 | **적용 + 드리프트 검사** | `upgrade head` → autogenerate 한 번 더 → **빈 diff 확인** 후 그 파일 삭제 | 모델과 DB가 어긋나면 다음 마이그레이션이 멀쩡한 걸 지우려 든다 (실제로 겪은 사고) |
| 7 | **실호출 검증** | `scripts/check_auth.py` 38케이스 | 성공 경로뿐 아니라 **막혀야 할 것이 막히는지**를 같이 본다 |
| 8 | **문서 갱신 → 커밋** | 계약서 §2 개정 · `PROGRESS` · `DEVELOPMENT_LOG` · `.env.example` | 중간에 쓰면 결정 바뀔 때마다 다시 씀. 단 **커밋 전엔 반드시** — 계약 변경을 팀이 모르면 옛 스펙으로 개발한다 |

### 5-3. 검증 방식 — "막혀야 할 것이 막히는가"

pytest는 아직 없다(8단계 과제). 대신 **실제로 뜬 서버에 호출하는 스크립트**를 단계마다 남겼다.

| 스크립트 | 무엇을 확인 |
|---|---|
| `scripts/check_tour_api.py` | TourAPI 키 연동·경로·오퍼레이션이 실제로 동작하는지 |
| `scripts/check_auth.py` | 인증 38케이스 — **성공 9 + 거부 29** |

거부 케이스를 더 많이 넣은 게 의도다. 인증에서 진짜 위험한 건 "로그인이 안 되는 것"이 아니라
**"막혔어야 할 게 통과하는 것"**이기 때문이다.

### 5-4. 문서 체계 — 5개가 각자 다른 질문에 답한다

| 문서 | 답하는 질문 | 갱신 시점 |
|---|---|---|
| `PROJECT_REPORT.md` | 전체 로드맵과 제약이 뭐지? | 단계 완료 시 |
| `PROGRESS.md` | **지금** 어디까지 됐지? | 단계마다 (스냅샷) |
| `DEVELOPMENT_LOG.md` | 그때 **왜** 그렇게 했지? | 단계마다 (누적, 절대 안 지움) |
| `API_CONTRACT.md` | 프론트·AI가 뭘 부르면 되지? | **구현 전에** 먼저 |
| `PROJECT_STRUCTURE.md` | 이 파일은 왜 여기 있지? | 구조 변경 시 |
| **`DEV_SUMMARY.md`** (이 문서) | 처음부터 지금까지 어떻게 온 거지? | 큰 변곡점마다 |

> ⚠️ 현재 `PROJECT_REPORT.md`는 **1단계 기준에서 멈춰 있다**(12테이블·자체JWT 표기 등이 옛 정보).
> 문서 스스로도 §8 말미에 그 사실을 적어두고 있다. 정리 필요.

---

## 6. 작업 설계도 ★

### 6-1. 요청이 흐르는 경로

```
  [모바일 앱]                    [AI 챗봇 (팀원)]
      │                                │
      │  Authorization: Bearer <access> │
      ▼                                ▼
 ┌──────────────────────────────────────────────────────┐
 │  app/main.py  — 라우터 등록 한 곳                      │
 └──────────────────────────────────────────────────────┘
      │
      ▼
 ┌──────────────────────────────────────────────────────┐
 │  features/*/router.py   ← 얇게. HTTP 입출구만          │
 │      · 쿼리·본문 검증은 schema.py(Pydantic)가 자동     │
 │      · 로그인 필요하면 deps의 CurrentUser 한 줄        │
 └──────────────────────────────────────────────────────┘
      │
      ▼
 ┌──────────────────────────────────────────────────────┐
 │  features/*/service.py  ← 판단·질의가 전부 여기         │
 └──────────────────────────────────────────────────────┘
      │                              │
      ▼                              ▼
 ┌──────────────────┐      ┌──────────────────────────┐
 │ models/ + PostGIS│      │ integrations/tour_api.py  │
 │ (우리 데이터)     │      │  → KTO TourAPI 실시간     │
 └──────────────────┘      │  → 호출 즉시 api_call_logs│
                           └──────────────────────────┘
      ▲                              ▲
      └───── core/ (설정·DB엔진·암호) ─┘
```

**핵심은 두 가지다.**

1. **라우터는 얇고, 판단은 service에 모인다.** 라우터를 보면 "이 API가 무슨 에러를 낼 수 있나"만
   읽히고, "왜 그 에러인가"는 service에 있다.
2. **외부 호출은 반드시 `integrations/tour_api.py`를 지난다.** 그 안의 `_get`이 호출을 자동으로
   `api_call_logs`에 기록한다 — **빠뜨릴 수 없는 구조**로 만든 것이 §2-1 요건 대응의 핵심이다.

### 6-2. 폴더 구조 — 기능 하나 = 폴더 하나

```
app/
├─ main.py              라우터 등록 (새 기능 = 여기 한 줄 추가)
│
├─ features/            ★ 기능별. 폴더 = router.py + service.py + schema.py
│   ├─ auth/            회원가입·로그인·로그아웃·소셜      (3단계 ✅)
│   │   └ + social.py(구글·카카오 검증) · mailer.py(인증코드)
│   ├─ contents/        작품 검색·상세                    (5단계 ✅)
│   ├─ places/          촬영지 조회·반경·상세 + matching.py (4·5단계 ✅)
│   ├─ regions/         지역 리졸브·목록                   (5단계 ✅)
│   ├─ health/          헬스체크                          (1단계 ✅)
│   └─ courses/         ← 6단계에 여기 추가된다
│
├─ integrations/        외부 연동 어댑터 ('기능'이 아니다)
│   ├─ tour_api.py      KTO TourAPI 클라이언트
│   └─ call_log.py      호출 입증 기록
│
├─ core/                설정 · DB 엔진 · 암호 원시도구
│   ├─ config.py        .env 로딩 (비밀값은 전부 여기로)
│   ├─ db.py            async engine · Base · get_db
│   └─ security.py      JWT 2종 · refresh 해시 · bcrypt
│
├─ deps/                CurrentUser · OptionalUser · DbSession
├─ shared/              Location · RegionRef · Page (공통 응답 모양)
└─ models/              ★ 옮기지 않음 — Alembic이 읽고, mappings가
                          contents·places를 동시에 참조해 어느 기능에도 안 속함
```

**`social.py`는 왜 `integrations/`가 아니라 `features/auth/` 안에 있나** —
TourAPI는 여러 기능(places·courses)이 쓰는 공용 어댑터지만, **소셜 검증은 인증만 쓴다.**
쓰는 곳이 하나면 그 옆에 두는 게 맞다.

### 6-3. 데이터 소유 경계 ⚠️ (컴플라이언스의 핵심)

**"무엇을 저장하고 무엇을 저장하지 않는가"**가 이 프로젝트에서 가장 중요한 설계선이다.

```
┌─ 우리 DB에 저장한다 ────────────────────┐   ┌─ 저장하지 않는다 ─────────────────┐
│                                          │   │                                    │
│  ★ 작품↔장소 매핑 (우리 큐레이션 자산)     │   │  TourAPI 응답 본문                 │
│    contents · places · mappings          │   │   · 운영시간 · 전화 · 개요          │
│    ← data.csv(KMDb+TMDB) 13,761행        │   │   · 대표이미지                     │
│                                          │   │                                    │
│  외부 참조 ID만                           │   │  → 매 요청 실시간 호출              │
│    tour_content_id · kmdb_code · tmdb_id │   │  → GET /places/{id}가 그때 부른다   │
│    poster_url (URL 문자열)                │   │                                    │
│                                          │   │  serviceKey (인증키)                │
│  사용자 데이터                            │   │  → .env에만. 로그·DB 어디에도 없음  │
│    users · courses · refresh_tokens      │   │                                    │
│                                          │   │  raw GPS 좌표                      │
│  호출 입증 로그 (본문 제외)                │   │  → 입력은 place_id/region_id 정수   │
│    api_call_logs                         │   │                                    │
└──────────────────────────────────────────┘   └────────────────────────────────────┘
```

왼쪽이 **차별점**이고 오른쪽이 **실격 방지선**이다. `places` 테이블에 운영시간 컬럼이 없는 것은
빠뜨린 게 아니라 **의도적으로 안 만든 것**이다.

### 6-4. 인증 토큰이 오가는 방식

```
① 로그인/가입    앱 ──→ 서버 ──→ DB      access(1h) + refresh(30d) 발급
                                          refresh는 SHA-256 해시로만 저장
                                          (원문은 이 응답에 한 번 실려 나가고 끝)

② 평소 호출      앱 ──→ 서버              Authorization: Bearer <access>
                                          서명만 검증 → DB 조회 없음(빠름)

③ access 만료    앱 ←──→ 서버 ──→ DB      401 → /auth/refresh → 새 한 쌍
                                          옛 refresh는 즉시 폐기(회전)
                                          ⚠️ 앱은 새 refresh로 교체 저장 필수

④ 폐기된 refresh 앱 ──✗ 서버              탈취로 간주 → 그 계정 전 세션 차단
   재등장                                  (정상 앱은 새것을 받아 갔으므로
                                           옛것을 다시 쓸 이유가 없다)

⑤ 로그아웃      앱 ──→ 서버 ──→ DB       그 refresh에 폐기 표시
                                          남은 access는 최대 1시간 더 유효
```

**프론트가 구현할 것은 ③ 하나다.** HTTP 클라이언트 인터셉터에
*"401이면 refresh 부르고 원래 요청 재시도"*를 한 번만 심으면 화면 코드는 토큰을 몰라도 된다.

---

## 7. 현재 개발 상태

### 단계별

| 단계 | 내용 | 상태 |
|---|---|---|
| 0 | 설계 (v1 Supabase → v2 검증 → v3 FastAPI) | ✅ |
| 1 | 프로젝트 뼈대 + `/health` | ✅ |
| 2 | DB 연결 + Alembic + 모델 | ✅ |
| 2.5 | 스키마 확장 + 실데이터 적재 (CSV 13,761행) | ✅ 2026-08-15 |
| **3** | **회원가입·로그인·로그아웃 + 소셜** | ✅ **2026-08-22** |
| 4 | place-detail — TourAPI 실시간 + 호출 입증 ⚠️합격핵심 | ✅ 2026-08-16 |
| 5 | 검색·지도 엔드포인트 | ✅ 2026-08-15 |
| 6 | 코스 추천 엔진 | ⬜ |
| 7 | AI 연동 계약 (`/courses/recommend`) | ⬜ 6단계에 포함 |
| 8 | 테스트·컴플라이언스 하드닝 | ⬜ |
| 9 | 배포·제출 준비 | ⬜ |

### 엔드포인트 19종

| 도메인 | 경로 | 단계 |
|---|---|---|
| 인증 | `POST /auth/email/send-code` · `/email/verify-code` · `/signup` · `/login` | 3 ✅ |
| 인증 | `POST /auth/google` · `/kakao` · `/refresh` · `/logout` · `/logout-all`🔒 · `GET /auth/me`🔒 | 3 ✅ |
| 작품 | `GET /contents/search` · `POST /contents/resolve` · `GET /contents/{id}` · `/{id}/places` | 5 ✅ |
| 지역 | `GET /regions/resolve` · `GET /regions` · `GET /regions/{id}/places` | 5 ✅ |
| 장소 | `GET /places?near=&radius_km=` | 5 ✅ |
| 장소 ⚠️ | `GET /places/{id}` — **TourAPI 실시간 상세** | 4 ✅ |
| 코스 | `POST /courses/recommend` · `POST/GET/DELETE /courses` | 6 ⬜ |

### DB (리비전 `9a1c7d2e5b40`, 10테이블)

| 테이블 | 행수 |
|---|---|
| `regions` | 244 (시도 17 + 시군구 227) |
| `contents` | 1,694 |
| `places` | 9,811 |
| **`content_place_mappings`** ★ | **13,755** |
| `users` · `courses` · `course_places` | 0 |
| `api_call_logs` (4단계) | 호출마다 누적 |
| `refresh_tokens` · `email_verifications` (3단계) | 0 |

데이터 품질 — 장소 좌표 100% · 장소↔지역 연결 100% · 지역 중심점 100% · 장르 82% · 포스터 82%

---

## 8. 남은 일 (마감까지 30일)

### 기능

| | 내용 |
|---|---|
| **6단계 코스 추천** | `POST /courses/recommend`(저장 안 함) + 코스 저장·조회·삭제🔒. 인증이 끝나 `CurrentUser` 한 줄로 붙는다. 동선은 PostGIS 직선거리 → 필요 시 Kakao Mobility/ODsay 보강 |
| 8단계 테스트 | **pytest 0개.** 지금은 실호출 스크립트뿐이라 서버를 띄워야 검증된다 |
| 9단계 배포 | 다국어(ko/en) 입증 · 데모 시나리오 |

### 설정 (배포 전 필수)

| | 내용 |
|---|---|
| ⚠️ `GOOGLE_CLIENT_ID` · `KAKAO_APP_ID` | 비어 있으면 **"이 토큰이 우리 앱 것인가" 검증을 건너뛴다** → 다른 앱 토큰으로도 로그인된다 |
| SMTP 계정 | 미설정. 지금은 인증코드가 서버 로그와 응답 `dev_code`로 나온다(개발 가능). Gmail 앱 비밀번호를 `.env`에 넣으면 코드 수정 없이 실제 발송 전환 |
| 운영계정 신청 여부 | 개발계정 **1,000건/일**로 심사·시연 트래픽을 감당할 수 있는지 확인 필요. 승인에 시간이 걸린다 |

### 데이터·품질

| | 내용 |
|---|---|
| 사전매칭 배치 완주 | `tour_content_id` 현재 11/9,811. 인기 촬영지 300~500곳을 하루 여러 번 나눠 채워야 한다 |
| **드라마가 10편뿐** | 앱 컨셉이 "영화·드라마"인데 CSV가 KMDb(영화DB) 출신이라 사실상 영화만 있다. 스키마는 **INSERT만으로 추가 가능**하게 설계해뒀으므로 데이터 확보 여부가 관건 |
| 포스터 없는 작품 296편(18%) | 프론트에 기본 이미지 필요 |

### 팀

- `eunseo` 브랜치 push·병합 시점 합의. `origin/yoon`이 `main`보다 뒤처진 시점에서 갈라져 있어,
  팀원이 `main`을 먼저 머지하면 충돌이 줄어든다.
- `PROJECT_REPORT.md`가 1단계 기준에서 멈춰 있어 정리 필요.

---

*갱신 이력 — 2026-08-22 최초 작성(3단계 완료 시점).*
