# Web framework
fastapi
uvicorn[standard]
pydantic-settings

# Stage 2: Database (SQLAlchemy async + PostgreSQL driver + migrations + PostGIS types)
sqlalchemy[asyncio]
asyncpg
alembic
geoalchemy2

# Stage 3: Auth (자체 JWT 발급/검증) + 소셜 토큰 검증·외부 API용 HTTP 클라이언트
python-jose[cryptography]
httpx
# ※ 소셜 로그인 전용이라 비밀번호 해싱(passlib/bcrypt)은 쓰지 않음.
