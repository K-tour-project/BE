# Web framework
fastapi
uvicorn[standard]
pydantic-settings

# Stage 2: Database (SQLAlchemy async + PostgreSQL driver + migrations + PostGIS types)
sqlalchemy[asyncio]
asyncpg
alembic
geoalchemy2

# Stage 3: Auth (자체 JWT 발급/검증) + 소셜 토큰 검증·외부 API용 HTTP 클라이언트
python-jose[cryptography]
httpx
# 일반 회원가입(이메일+비밀번호) 비밀번호 해싱. passlib은 유지보수가 멈춰 bcrypt를 직접 쓴다.
bcrypt
# pydantic의 EmailStr 검증에 필요 (없으면 import 시점에 에러)
email-validator
